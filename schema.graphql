type Product {
  id: ID
  name: String
  description: String
  price: String
  stockQtt: Int
}

"""Root query"""
type Query {
  users: [User]
  products: [Product]
}

type User {
  id: ID
  name: String
}

