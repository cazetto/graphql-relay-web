type Product {
  id: ID
  name: String
  description: String
  price: String
  stockQtt: Int
}

"""Root query"""
type Query {
  users: [User]
  products: [Product]
  me: User
}

type User {
  id: ID
  firstName: String
  lastName: String
  userName: String
}

